{
  "name": "root",
  "private": true,
  "version": "3.4.1",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "npm start --prefix=./packages/jsbattle",
    "start:dev": "lerna run start:dev",
    "clean": "lerna run clean --stream",
    "lint": "lerna bootstrap --hoist && lerna run lint --stream",
    "build": "lerna bootstrap --hoist && lerna run build --stream",
    "test": "lerna bootstrap --hoist && lerna run test --stream --concurrency 1",
    "all": "lerna bootstrap --hoist && npm run-script clean && npm run-script lint && npm run-script build && npm run-script test",
    "ci": "lerna bootstrap --hoist && npm run-script clean && npm run-script lint && npm run-script build && npm run-script test",
    "commit": "git-cz",
    "update": "for d in ./packages/*; do npm-check-updates -i --packageFile $d/package.json; done",
    "audit": "lerna-audit",
    "heroku-cleanup": "rm -rf .jdk node_modules/git-cz node_modules/canvas/ && du -h  -t 20M . | sort -hr"
  },
  "engines": {
    "node": "14"
  },
  "devDependencies": {
    "commitizen": "^4.2.2",
    "cz-conventional-changelog": "^3.3.0",
    "dotenv": "^10.0.0",
    "git-cz": "^4.7.6",
    "lerna": "^3.22.1",
    "lerna-audit": "^1.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "npm-upgrade": "^2.0.4",
    "passport-local": "^1.0.0"
  }
}